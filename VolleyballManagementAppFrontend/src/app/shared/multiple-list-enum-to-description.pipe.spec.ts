import { MultipleListEnumToDescriptionPipe } from './multiple-list-enum-to-description.pipe';

describe('MultipleListEnumToDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleListEnumToDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
